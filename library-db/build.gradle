apply from: "$rootProject.projectDir/buildScript/android_db_lib_build.gradle"

android {

    defaultConfig {
        versionCode rootProject.versionCode
        versionName rootProject.versionName
    }

}