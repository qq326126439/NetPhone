apply from: "$rootProject.projectDir/buildScript/android_network_lib_build.gradle"

android {

    defaultConfig {
        versionCode rootProject.versionCode
        versionName rootProject.versionName
    }

}